// Fonction pour actualiser la page toutes les 6 secondes
function actualiserPage() {
    location.reload();
  }
  
  // Appeler la fonction toutes les 3 secondes
  setInterval(actualiserPage, 3000); 
  